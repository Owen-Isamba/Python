#Write a program to display "Login successful" if the user has entered the right username and password, otherwise print "Invalid credentials"
un=(input("Enter username:"))
pw=int(input("Enter password:"))
a=(input("Password:"))
userInput=input("What is your username:")
if userInput==un:
    if a==pw:
        print("Login successful")
else:
    print("Invalid credentials")
